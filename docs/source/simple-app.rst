Simple App
==========

anyldap uses AnyIO for asynchronous networking. Start with the working client
and server examples in :doc:`quickstart`. The protocol classes also expose
``*_async`` methods for binding, searching, and changing directory entries.
