Simple App
==========

This page previously walked through a legacy event-loop integration.

The current project runtime is AnyIO-based. For a minimal client/server starting
point, use the examples in ``docs/source/examples`` that import ``anyio`` and
the ``*_async`` APIs from ``anyldap.protocols.ldap``.
